{
  "name": "ffext-deploy",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix web run build",
    "deploy": "npm run build && wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
